# Test-only dependencies. Install alongside requirements.txt to run the suite.
pytest>=8.0.0

# FastAPI's TestClient is built on httpx and imports it at module level, so the
# tests cannot even collect without it. The runtime no longer needs httpx, which
# is why it lives here rather than in requirements.txt.
httpx>=0.27.0
